ext {
    // Koin
    koin_version = '3.1.4-alpha-1'
    // TODO pass it to true to make release
    is_snaptshot = true

    // Kotlin
    kotlin_version = '1.5.31'
    coroutines_version = "1.5.2-native-mt"

    // Dokka
    dokka_version = '1.4.32'

    // Ktor
    ktor_version = '1.6.4'

    // Test
    junit_version = "4.13.2"
    junit5_version = "5.6.2"
    mockito_version = "3.5.9"
    mockk_version = "1.10.0"

    // Doc
    // asciidoctor_version = "1.5.3"
    // asciidoctor_pdf_version = "1.5.0-alpha.15"

    // Publish
    // android_maven_publish = "3.6.3"

    // Ksp
    koin_ksp_version = "1.0.0-alpha-1"
    ksp_version = "1.5.31-1.0.0" 
}